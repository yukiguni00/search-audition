# オーディションEAST 出演者検索 データ運用ルール

このファイルは、サイトのデータ更新・掲載判断を今後の作業者やChatGPTへ引き継ぐための編集方針です。

## 1. 掲載範囲の基本方針

このサイトは、劇場所属メンバーを網羅するための公演データベースではありません。
主な対象は、オーディションやチャレンジバトルなどを通じて劇場所属を目指す芸人、またはそのルート上にいる芸人です。

そのため、グランドバトル等で劇場所属メンバー全体の出演者が公式発表されていても、原則として全員を追加しません。
オーディション経由・チャレンジバトル経由など、このサイトで追跡しているルートの出演者のみを掲載します。

例外として、以前は劇場所属だった芸人がバトル等で降格し、再びオーディションやチャレンジバトルの対象になった場合は掲載対象に含めます。

「公式には他の出演者も発表済みだが、このサイトでは掲載範囲外」という状態は正常です。
そのため、サイト上に「その他出演者は発表され次第更新します」のような、網羅掲載を前提とする注釈は原則として表示しません。

## 2. 情報源と確定度

- 公式発表を最優先します。
- 香盤画像が低解像度で芸名を判読できない場合は推測で確定しません。
- 既存JSONに似た名前があっても、画像の字形より優先しません。
- 出演者名が不明な場合は保留し、ユーザーによる目視確認を優先します。
- 外部・第三者サイトの情報は、ユーザーが正式情報として採用すると明示した場合を除き、公式情報と同等には扱いません。

## 3. イベントJSONの正本

月別ファイル `data/events/YYYY-MM.json` を正本とします。
`data/events.json` と `data/events-index.json` は同期生成物です。

更新後は可能な限り以下を確認します。

```bash
npm run data:sync
npm run data:check
node --check script.js
```

`data/events.json` だけを手作業で編集しないでください。

## 4. 複数公演イベント

複数日・複数部を1枚のカードにまとめる場合は `sessions` を使用します。

- 親イベントの `performers` は、出演部未定など親カードに直接表示する必要がある場合のみ使用します。
- 出演部が確定したら `sessions[].performers` に移します。
- 部ごとの出演者が未発表でも、掲載範囲外の劇場所属メンバーまで補完しません。

## 5. 進出結果

オーディションや2ndステージから次のバトルへ進出する場合は、既存の `stageResults` の形式を踏襲します。
次回日程・会場・出演部などが未定の場合は推測で埋めず、未定のまま保持します。

## 6. プロフィールリンク

`data/profiles.json` は公式プロフィールURLの対応表です。
吉本公式プロフィールが確認できない若手芸人については、無理に類似名や個人ページへ紐づけません。

## 7. 表記・履歴を残す考え方

その時点の公式発表に使われていた名称や結果ラベルは、後から別名称になっても履歴として残すことがあります。
過去情報を現在の呼称へ一律置換せず、表示上の利便性とのバランスを取ります。

## 8. 重要な編集原則

- 最新GitHubの内容を正本として作業する。
- ユーザーが確定した芸名表記を優先する。
- 分からない出演者名を推測しない。
- 「掲載されていない = 開催されていない / 出演していない」とは限らない。
- サイトの目的を超えて、劇場所属メンバー全体の網羅データベースへ拡張しない。
